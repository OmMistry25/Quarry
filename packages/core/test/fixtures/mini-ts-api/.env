# Fixture only. Deliberately checked in so Quarry's S1 secret-stripping tests have
# something to strip. These values are placeholders and grant access to nothing.
PORT=3000
DATABASE_PATH=./inventory.db
SESSION_SECRET=placeholder-not-a-real-secret
WAREHOUSE_API_TOKEN=placeholder-not-a-real-token
