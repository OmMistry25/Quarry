{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "description": "Quarry demo UI \u2014 paste a repo URL, pick a role, download a verified package.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "pnpm --filter core build && next build",
    "start": "PATH=\"$PWD/node_modules/.bin:$PATH\" next start -H 0.0.0.0 -p ${PORT:-3000}",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.1.237",
    "core": "workspace:*",
    "next": "^14.2.33",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1"
  }
}
