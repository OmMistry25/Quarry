# Fail installation when a dependency declares a Node engine range this workspace does not
# satisfy. execa 10 quietly required Node >=22 while package.json declares >=20; without this
# it installed cleanly, passed on a Node 22 dev machine, and only broke in CI.
engine-strict=true
